services:
  - type: static
    name: realestate-hub-frontend
    env: static
    plan: free
    buildCommand: npm install && npm run build
    publishPath: dist
    rootDir: client
    routes:
      - type: rewrite
        source: "/*"
        destination: "/index.html"
